Description:
    Generates an user along with a login.

Example:
    `./script/generate login [username [name [e-mail]]]`

Defaults:
    username  => ENV['USER']
    name      => username.titleize
    e-mail    => "#{ username.demodulize.underscore }@#{ `hostname`.chomp }"
