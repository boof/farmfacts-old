<br style="clear: both;" />

<h2>Edit Article</h2>

<% remote_form_for [:content_management, @article], :url => content_management_news_path, :method => :put, :loading => '$("commit").disable();$("flash").update("Updating...")' do |form| %>
	<%= render :partial => 'form', :object => form %>
	<p>
		<%= button_to_function 'Back', 'window.location = "/content_management/news"' %>
		<%= submit_tag 'Update', :id => 'commit' %>
		<span id="flash"></span>
	</p>
<% end %>
