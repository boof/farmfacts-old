= link_to 'Done', admin_pages_path
- @elements.each do |element|
  = link_to image_tag(element.icon_path, :size => '190x120'), new_admin_themed_page_element_path(@themed_page), :title => element.path
