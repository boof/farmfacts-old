- render_navigation :class => 'box' do
  .last= link_to '&laquo; Navigation', params[:return_to]

- form_for :node, @node, :url => admin_navigation_node_path(@node.container_id, @node), :html => {:method => :put} do |f|
  = render :partial => 'form', :object => f
  = submit_tag 'Update'
